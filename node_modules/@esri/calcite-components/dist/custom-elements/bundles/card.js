import { CalciteCard, CalciteLoader, CalciteCheckbox, CalciteLabel } from "../index";
import { register } from "../utils";
register({
    "calcite-card": CalciteCard, "calcite-loader": CalciteLoader, "calcite-checkbox": CalciteCheckbox, "calcite-label": CalciteLabel
});
