import { CalcitePopover, CalciteIcon, CalcitePopoverManager } from "../index";
import { register } from "../utils";
register({
    "calcite-popover": CalcitePopover, "calcite-icon": CalciteIcon, "calcite-popover-manager": CalcitePopoverManager
});
